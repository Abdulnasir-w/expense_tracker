import 'package:flutter/material.dart';

class Transiction extends StatelessWidget {
  const Transiction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Transiction Screen"),
      ),
    );
  }
}
